# UI Components

Primitive, reusable UI building blocks.

- Buttons, inputs, modals, cards
- Design system components
- Should be stateless and composable
- Located in a dedicated package or local library
