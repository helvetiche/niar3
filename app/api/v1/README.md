# API v1

Versioned API routes. Keeps backward compatibility as API evolves.

- `/api/v1/*` - v1 endpoints
- Add `/api/v2/*` when breaking changes are needed
- Apply auth and rate limiting at route or proxy level
