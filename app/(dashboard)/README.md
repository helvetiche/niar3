# Dashboard Route Group

Protected application area. Requires authentication.

- All routes under `(dashboard)` are **protected**
- Use Server Layout Guards to enforce auth
- Shared dashboard layout (sidebar, header) lives here
