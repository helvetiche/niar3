# App-Level Components

Components used only within the App Router (layouts, route-specific UI).

- Co-located with routes when feature-specific
- Shared across route groups when needed
- Prefer `components/` for truly global components
