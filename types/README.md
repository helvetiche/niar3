# Global Types

Shared TypeScript types and interfaces.

- Domain models
- API request/response shapes
- Extend `next-env.d.ts` for module declarations
- Feature-specific types live in `features/[name]/types/`
