# Firebase Admin SDK

Server-only. Uses service account credentials. Never bundled to client.

- Verify ID tokens from clients
- Create custom tokens
- Privileged Firestore operations (bypass Security Rules)
- User management (create, delete, update)

Use `'server-only'` package. Import only in:
- API Route Handlers
- Server Components
- Server Actions
