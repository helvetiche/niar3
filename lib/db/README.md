# Database

Supplemental database clients (PostgreSQL, etc.).

- **Primary data**: Use Firestore via `lib/firebase` and `lib/firebase-admin`
- This folder is for non-Firebase data stores if needed
- Single client instance, connection pooling
