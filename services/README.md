# Services

API and external service clients.

- HTTP clients (fetch wrappers)
- Third-party API integrations
- Keep services thin—orchestration in features or API routes
- Use for server-side only; client calls go through API routes
